# Core
requests>=2.32,<3
numpy>=2.4.6,<3
Pillow>=10.4

# PDF rendering — pure-Python wheel, no poppler / system binaries needed
pymupdf>=1.24

# OCR engine (PP-OCRv6, requires PaddleOCR >= 3.7)
paddlepaddle>=2.6
paddleocr>=3.7

# Optional: uncomment to enable OCR_ENGINE=openvino (faster on Intel CPUs)
# openvino>=2024.4
