# Copy this file to .env and fill in your values.
# .env is loaded automatically by `docker compose` and is git-ignored.

# --- Required -------------------------------------------------------------
PAPERLESS_API_TOKEN=changeme

# --- Optional overrides (defaults live in docker-compose.yml) -------------
# PAPERLESS_BASE_URL=http://paperless-ngx:8000
# PAPERLESS_PROCESSING_TAG=paddle_processing
# PAPERLESS_TRACKING_TAG=paddle_processed
# OCR_LANG=en
# OCR_TIER=small
# OCR_ENGINE=paddle
